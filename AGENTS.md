# 제품: LiveToEat — 사람을 통해 발견하는 맛집 지도

React Native로 만드는 국내 서비스용 iOS/Android 앱이다. 내 맛집을 저장하고, 다른 사람의 공개 지도를 둘러보고, 원하는 지역의 장소만 골라 공유한다. 제품명은 LiveToEat이다. 첫 출시는 대한민국을 대상으로 하며 한국어/영어 UI와 확장 가능한 지역 모델을 유지한다.

## 제품에서 지킬 것

- `내 지도`에는 내 저장만 표시한다. 타인의 저장은 `둘러보기`와 상대방 지도에서 본다.
- 다른 사람의 장소를 내 지도에 저장할 수 있다. 그 사람의 개인 메모와 방문 기록은 복사하지 않는다.
- 국내 지역으로 보기/공유를 지원한다. 국가 코드와 가변 깊이 지역 구조는 유지하되 해외 검색과 출시는 첫 출시 범위에서 제외한다.
- 기본 저장은 비공개다. 공개 지도, 링크 한정 공유, 개인 메모를 분리한다.
- 지역 공유는 선택한 장소의 스냅샷이다. 이후 저장을 자동 추가하지 않는다. 삭제/비공개 전환/철회는 다음 조회부터 반영한다.
- 앱이 없어도 허용된 공유 지도를 웹에서 볼 수 있다. 전체 웹앱을 만들라는 뜻은 아니다.

## 구현 경계

npm과 package-lock.json, React Native + Expo development build + TypeScript, Supabase를 사용한다. 2026-09-13 확정한 설계는 iOS/Android 네이버 Maps, 네이버 지도 JavaScript API를 사용하는 작은 공유 웹, 공공데이터 기반 자체 장소 DB다. 앱 연결은 `@mj-studio/react-native-naver-map`을 우선 검증하며 현재 Expo/RN 호환 버전은 실기기 검증 후 고정한다. 장소 검색과 상세 조회는 서버에서 자체 DB를 읽는다.

자체 장소 ID, 공공데이터 원본 ID·출처·자료 기준일, 사용자 작성 기록을 분리한다. 기본 자료는 소상공인시장진흥공단 상가(상권)정보이며 인허가 데이터는 품질 검증 후 보완한다. 네이버/카카오 검색은 선택적 보조 후보이며 검색 응답을 자체 장소 DB로 영구 복제하는 전제를 두지 않는다. 자료 갱신·폐업·중복 병합 때도 사용자 저장과 메모를 보존한다. 비밀키와 세션을 클라이언트 공개 설정이나 로그에 넣지 않는다.

현재 코드는 새 설계로 전환하기 전이다. 문서의 설계 확정과 SDK 교체·데이터 적재·DB/API 전환·실기기 검증 완료를 구분한다. 다음 우선 작업은 국내 장소 데이터 표본 품질과 양 플랫폼 네이버 지도 호환성 검증이다.

완료는 목업이 아니라 실제 계정, DB, 지도, 공유, 권한 테스트와 양대 앱스토어 배포로 판단한다. 유료 서비스 가입, 계정 생성, 프로덕션 변경, 스토어 제출은 소유자 승인 없이 실행하지 않는다.

## 상세 문서

| 읽을 때 | 문서 |
|---|---|
| 기능/화면/범위를 정할 때 | [제품 명세](docs/01_PRODUCT.md) |
| 앱/DB/API/보안을 구현할 때 | [구조와 데이터](docs/02_ARCHITECTURE.md) |
| 네이버 지도/자체 장소 검색/공공데이터/지역을 구현할 때 | [지도와 장소 검색](docs/03_MAPS_AND_IMPORT.md) |
| 개발 시작/다음 작업/검증 기록 | [개발계획](docs/04_DEVELOPMENT.md) |
| 테스트/스토어 출시/운영 | [출시와 운영](docs/05_RELEASE.md) |

먼저 제품 명세와 개발계획의 현재 상태를 읽고, 해당 작업의 문서만 추가로 읽는다. 완료/미완료와 결정 변경은 개발계획에 갱신한다. 불필요한 새 MD 파일을 만들지 않는다.
